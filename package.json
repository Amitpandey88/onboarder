{
  "name": "codebase_onboarder",
  "version": "0.1.0",
  "private": true,
  "description": "Drop a path. Get a map. A local tool that draws how a repository fits together.",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
